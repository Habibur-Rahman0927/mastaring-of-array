// join Method

const a = ['Habib','Rasel', 'khan'];

console.log(a.join());
console.log(a.join(', '));
console.log(a.join(' + '));
console.log(a.join(''));


// includes, indexOf, lastIndexOf
const arr = ['one', 'Two', 'Three','one'];
// Includes
console.log(arr.includes('one')); // return ture or false

// indexOf
console.log(arr.indexOf('one'));

// lastIndexOf
console.log(arr.lastIndexOf('one'));
